@{
  Layout = "_Layout";
}

@model TreatTracker.Models.Flavor

<h2>Flavor Deets</h2>

@using (Html.BeginForm())
{
  @Html.LabelFor(model => model.Name)
  @Html.TextBoxFor(model => model.Name)
  <br>
  <input type="submit" value="Create Flavor" />
}